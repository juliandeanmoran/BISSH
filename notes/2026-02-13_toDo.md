### IEI-project to do
### Julian Moran
### 2026-02-13


1. Build script for iteratively querying RCSB PDB alignment API

2. Clarify what `Query_length`, `Target_length` cols denote
    - currently filtering by `Overlap_ratio`, which is computed from these cols
    - potentially stop this filter layer

3. Convert `print(f"...")` calls to `logger.info(f"...")` calls     x
    - `test_rcsb_pdb_align_api.ipynb`                               x
    - `explore_aJain_pipeline.ipynb`                                x