### BISSH: dev plan
### Julian Moran
### 2026-02-04


1. Build database for quick accession and querying
	- need to overcome problem of `(2.3 * 10^6)` by `(2.3 * 10^6)` E-Values
	- lookups to this graph are non-trivial
	- lead: V Furlan


2. Find all clusters with `LCAtaxID == target_val` and `sapGO == target_val`
	- need to pin down what `sapID` is in `foldSeek_3`
	- need to pin down what `cluFlag` is in `foldSeek_3`
	- need to pin down why there are only 180,000 rows in `foldSeek_3`

...

3. Need an algorithm for 3D structural alignment for downstream validation
	- OpenCV?
	- some pre-existing tool specific for PDB files?
	- maybe A Hajianpour knows